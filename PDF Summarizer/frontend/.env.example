# Copy to .env for the frontend build. Never commit .env.
#
# Base URL of the deployed backend API. Leave empty for local dev — the CRA
# dev proxy (package.json "proxy") forwards /api to http://localhost:5000.
# In production set this to your hosted backend, e.g. https://your-app.up.railway.app
REACT_APP_API_BASE_URL=
